# Repository-only metadata is not part of the runtime image.
.git
.github
README.md
LICENSE
NOTICE

# Local runtime config, deployment files, and state never enter Docker builds.
.env
.env.*
config
data
deploy

# Dependencies and development artifacts are not part of the runtime image.
node_modules
test
*.log
.DS_Store
